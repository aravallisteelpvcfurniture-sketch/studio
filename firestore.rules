
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    // Direct Admin check using both Email and UID
    function isAdmin() {
      return isSignedIn() && (
        request.auth.token.email == "aravallisteelpvcfurniture@gmail.com" || 
        request.auth.uid == "Qmcch2NXxmg47Zf28Wh0KTp9Njt1"
      );
    }

    match /quoteRequests/{requestId} {
      // Anyone can create an inquiry
      allow create: if true;
      // Only Admin can see the full list of inquiries
      allow read, list: if isAdmin();
    }

    match /users/{userId} {
      allow get, write: if isSignedIn() && request.auth.uid == userId;
    }

    match /products/{id} {
      allow read: if true;
    }

    match /carts/{userId}/items/{productId} {
      allow read, write: if isSignedIn() && request.auth.uid == userId;
    }
  }
}
